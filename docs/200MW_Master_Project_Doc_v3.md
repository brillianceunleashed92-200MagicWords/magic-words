# 200 Magic Words — Master Project Document (v3)
**Last updated:** July 6, 2026 · Production LIVE at https://200magicwordsapp.com
**Supersedes v2.** Delete all older master docs from the Project (see FILE HYGIENE below).

**Stack:** React/Vite · Supabase (`ozhqsaysltiamadpcruz`) · Vercel · ElevenLabs · Claude API · Stripe (TEST mode)
**Repo:** brillianceunleashed92-200MagicWords/magic-words · SSH push · Vercel auto-deploy
**Workflow:** launch `claude` from inside `~/magic-words` (never `cd` — see CLAUDE.md top rule). Autonomous-run setup: `.claude/settings.local.json` allowlist + a PreToolUse auto-approve hook; only `git push origin main` and `supabase db push` prompt. Helper scripts: `scripts/db-query.mjs <file.sql>` (DB reads), `scripts/admin-user.mjs create|delete` (test accounts), `scripts/idor-proof.mjs` (security regression).

---

## WHAT THIS APP IS
AI-powered early literacy app (ages 4–8) built on **Dr. Marion Blank's methodology**. 200 foundational words across 18 units, taught through adaptive games with ElevenLabs audio, a Candy Galaxy theme, and Nova the Comet Spark as mascot. Freemium: Units 1–5 free, 6–18 on Family Plan ($9.99/mo / $79/yr — Stripe checkout built + verified, still TEST mode).

### ⚠️ CORE METHODOLOGY RULE (non-negotiable, applies to every prompt)
Dr. Blank is **anti-phonics**. NEVER use sound-blending / sounding-out anywhere in the app (e.g., never "cuh-a-tuh" for "cat" — that distortion confuses early readers). Words are taught as **whole units tied to meaning and language**. Blank's actual techniques: "Find the Word" (say the whole word, find it among look-alikes), cloze-in-context (fill the blank in a sentence, then read it), "circle the word" (repeated meaningful exposure), whole-word recognition. Any activity that teaches recognition must use whole-word/meaning-first, never phoneme blending.

---

## CURRENT STATE — MERGED & LIVE
1. **Candy Galaxy v2 architecture** — componentized (`src/screens/`, `src/components/candy/`, `src/lib/`, `src/stores/`, `src/theme/tokens.js`), lazy-loaded routes, legacy monolith parked at `/app-legacy`.
2. **Guided Path (Option B)** — the activity screen is a single vertical journey: one glowing "current" step, completed steps with earned stars, locked upcoming steps, a trophy reward node. Composed only of activities VALID for the focus word (picture-matching only for `has_art` words; function words → context activities). Sequence uses a pedagogical order informed by the difficulty governor.
3. **Phase 2 Parent Loop** — multi-child profiles (child_id-keyed), Story Engine (vocab-validated AI stories), 4-tab Parent Portal (Dashboard + AI Insight, Moments + share cards, Mastery Map, Settings), time limits, all-200-word ElevenLabs audio.
4. **Security hardening (8 phases)** — full RLS audit/fixes (word_progress had RLS OFF; Stripe endpoints trusted client IDs; earn_sparks unbounded — all fixed, proven via `idor-proof.mjs` 9/9). JWT auth + per-user rate limits on every AI/TTS endpoint + TTS caching. Secrets scan clean. Input validation incl. prompt-injection fix. Security headers + CSP (**Report-Only**) in vercel.json (also restored the SPA rewrite, fixed /app 404). health-check reduced to `{"status":"ok"}`. webhook_failures + security_events tables. COPPA baseline: `COPPA_DATA_INVENTORY.md`, verified cascade account-deletion, parental-consent checkbox, DRAFT /privacy + /terms.
5. **Word-list unification** — session-generator is server-authoritative from the real 200-word `words` table: verifies child ownership, reads plan + word_progress server-side, selects by current unit + spaced-repetition + confidence sample. Free-tier Units 1–5 cap enforced server-side (forged requests proven ignored). `word_type` column (noun/verb/adjective/number/function). Old 18-word ALL_WORDS + 10-word fallback deleted.
6. **WordArt system** — hand-drawn SVGs with single source of truth: `words.has_art` + `wordArtManifest.json` + `check-wordart-sync.mjs` (wired into build). Picture-matching draws only from `has_art` words. NO letterforms in art (Word Hunt answer-leak rule). Batches 1 & 2 shipped (Units 1–10); undepictable words documented and routed to non-picture activities.
7. **Completion & celebration mechanism** — shared pipeline: `GameEngine` per-question `onProgress` → `PlayScreen.handleProgress` writes `learning_events` (promises pushed to `pendingLearningEventsRef`) → `onSessionEnd` awaits `Promise.allSettled(pending)` before the completion check / query invalidation. Mastery-celebration gate: tier-2 star ignition requires mastery ≥ 80 AND `attempt_count ≥ 3` (`isRealMastery`). `useTodayWordActivityQuery` uses `refetchOnMount:'always'`.
8. **Onboarding** ("Star Learner") — required name + avatar (errorless validation), stale/dead-session handling (FK-23503 → friendly re-auth).

---

## KEY REFERENCE
- **Migrations:** ~0034+. Tables: words (200: unit, word_type, teaching_track, audio_url, has_art; definition/image_url NULL), word_progress, learning_events, user_streaks, user_stats, user_sparks, child_profiles (incl. `placement_unit`/`measured_unit`/`placement_completed_at`, all service-role-write-only), parent_settings, subscriptions (incl. `created_at`, added Prompt 9), parent_child_links (unused by UI), teacher_classes/class_members (legacy-tree), api_rate_limits, webhook_failures, security_events, product_events (first-party analytics, service-role-only — see `ANALYTICS.md`), story_catalog. ON DELETE CASCADE verified across the child/user graph.
- **Stripe (TEST):** prod_UoN8JYjYwDK7lw; monthly price_1TokOI1HwJlEooq4y3crPkgC; yearly price_1TokOI1HwJlEooq4n13cOA46. Checkout/portal derive identity from verified JWT. Webhook failures → webhook_failures table.
- **AI endpoints (all JWT + rate-limited):** speak 60/min (cached by text hash), session-generator 10/min, ai-helper 30/min, story-engine 4/day, parent-digest 4/day.
- **Audio:** module-level singleton (`playAudio`/`currentAudio`), one sound at a time. CSP media-src allows `blob:`.
- **Tests/gates:** Playwright suite; `idor-proof.mjs` (16 real `check()` calls as of Prompt 9 — `/api/track` allowlist/identity checks added; see `LAUNCH_ANALYTICS_REPORT.md` for why prior prose said "10/10" when the file actually had 11 before this pass); `npm run build`; `check:no-emoji`; `check-wordart-sync`. Live-test buttons via `getByRole('button',{name})`, NOT forced clicks on text nodes (motion.button components).
- **Design law:** `DESIGN_BRIEF.md` — Candy Galaxy tokens only (--sky #5B4BD6 / --sky-deep #3D2FA8 / --sky-night #2B2080 gradient, --sun #FFC531, --mint #3EE0B8, --bubble #FF6FA5, --tang #FF8A4C, --cloud #FFF, --ink #2A2160), Baloo 2 (headers/numbers) + Quicksand (body), chunk shadows + press-down, errorless (no red/X), 44px+ targets, NO emoji in shipped UI.

---

## REPAIR SEQUENCE (active work — ordered)
A round of dad-testing surfaced ~19 issues + a set of product decisions. Being worked in this order:
1. **Prompt 1 — Group B safety net.** *Status: PARTIALLY DONE (merged).* ✅ FIXED: inflated stars (Word Song/Magic Video/Draw It/Word Builder were auto-reporting correct:true → always ★★★; now a fixed honest 1★ via `SCORELESS_GAME_TYPES` in `questProgress.js`; real-quiz activities keep accuracy-based 1/2/3). ✅ FIXED: exit-doesn't-save — real cause was **Story Time's `StoryReader.jsx` full-screen portal covering the exit button** (not Word Song); now renders its own close button via `onExit` prop threaded through StoryTimeActivity/GameEngine/StoryScreen. ❌ OPEN: mid-session celebration misfires (Match & Sort every answer / Word Hunt random / Say-It after word 6) — **could not reproduce** after extensive scripted + live testing incl. the mastery-threshold-crossing scenario; the mid-session "word mastered" celebrations that DO fire are by-design. These remain open and need an EXACT real-device repro (browser, muted?, precise tap sequence) to pin down. Note: `StoryReader.jsx` `onExit` is now the ONLY way to reach an exit control while its portal is open — future callers must pass it.
2. **Audio consolidation.** *Status: DONE (merged).* One voice throughout: gameplay uses ElevenLabs but UI/nav buttons (Home) use a different voice (likely browser `speechSynthesis`) — consolidate all to the single ElevenLabs voice. PLUS: correct answer plays SOUND ONLY (no spoken word); read-along highlight re-synced to narration; and add a missing **account affordance** — a "logged in" indicator (avatar circle w/ child's first initial) + a logout path placed BEHIND the Grown-Ups gate (a child must not be able to log themselves out).
3. **WordArt legibility pass.** *Status: DONE (merged).* ✅ Nova verb set shipped for all 9 standalone verbs (eat/swim/dance/sing/sleep/sit/fly/jump/run), replacing the old flat "Buddy" blob that made them read as near-identical — fixed run/jump/sit specifically (audit verdict WRONG), plus duck/shark noun art. ✅ Draw It now shows a WordArt reference image (had none before). ❌ Composed noun+verb scenes ("The dog can run" showing a dog running) were **descoped this pass** — the sentence-generation templates have no noun+verb pairing mechanism at all today (confirmed by reading the code), so building composed scenes first requires a sentence-content decision, not just art. See `docs/WORDART_HYBRID_REPORT.md` for the full assessment; flagged as a prerequisite for Fill the Story's Prompt 4 rebuild if composed scenes are still wanted.
4. **Activity rebuilds:**
   - **Fill the Story.** *Status: DONE (merged), see `docs/FILL_THE_STORY_REPORT.md`.* Killed the double-tap → single-tap-to-place; added a pre-answer picture-as-cue for `has_art` targets (WordArt shown WITH the question, no more post-answer reveal); verb templates rewritten Nova-subject ("Watch Nova ___!" etc., server+client mirrored); extended the WordMatch-style errorless scaffold (wiggle/soften/hint-glow, second-miss-completes) to this activity; added a read-back of the completed sentence after the success sound; distractors now hard-filtered to the target's word_type (was unit-preferred but not type-guaranteed) plus a picture-confusable-pairs exclusion list.
   - **Draw It → LETTER TRACING.** *Status: DONE (merged), see `docs/DRAW_IT_TRACING_REPORT.md`.* Freeform canvas replaced entirely: hand-authored ordered-stroke manifest for all 26 lowercase letters (`src/letters/letterStrokes.js`) with a build-time coverage check; letter-by-letter SVG tracing (animated stroke demo, green start dot + direction arrow, generous-tolerance pointer trace, off-path re-cue — never a fail state); per-letter mint tick; word-complete plays the whole word once via the shared singleton then the standard Nova celebration. `onAnswer` contract preserved byte-for-byte (still `SCORELESS_GAME_TYPES`). Whole screen moved off `gameTheme.js` onto Candy tokens (joins the `isE2Activity` chrome).
   - **Quiz Boss.** *Status: DONE (merged), see `docs/ACTIVITY_ROSTER_REPORT.md`.* Repurposed from learner self-rating ("I know it / need practice" — unreliable for this age, and never actually measured anything) to an app-measured 6-question review battle: server-authoritative review-only word pool (`api/session-generator.js`'s new `reviewOnly` mode, spaced-repetition/lowest-mastery-first selection), each question reusing Find the Word's audio mechanic or Word Hunt's picture mechanic per word, errorless scaffold, boss always defeated at the end.
   - **Word Song → "Find the Word".** *Status: DONE (merged), see `docs/ACTIVITY_ROSTER_REPORT.md`.* Blank's own technique: Nova speaks the whole word once (audio-only cue, never displayed), child finds it among 3 look-alike real words from a hand-curated 200-word manifest (`src/games/findTheWordManifest.js` + `check-findtheword-sync.mjs` build gate). NO phonics anywhere. Accuracy-based stars (was flat 1★).
   - **Magic Video → CUT.** *Status: DONE (merged), see `docs/ACTIVITY_ROSTER_REPORT.md`.* Non-functional stub removed from the rotation, `activityDefs`, and `SCORELESS_GAME_TYPES` entirely; historical `learning_events` rows with `game_type='magic_video'` confirmed to still read correctly everywhere (no reader gates on a known-gameType allowlist).
5. **Say It with Nova overhaul + polish pass.** *Status: DONE — automatable half (merged), see `docs/POLISH_PASS_REPORT.md` and `docs/DEVICE_TEST_CHECKLIST.md`.* Galaxy-map lock fixed and reproduced live (missing `inProgress` status between `locked`/`current` in `GalaxyScreen.jsx`'s per-word derivation — a word with `0 < mastery < 80` now shows its real percentage and stays tappable). `lessonChrome.jsx`'s `AnswerTile`/`ConfettiStars` now gate reduced-motion once at the primitive, so every consumer inherits it. Session-flow polish: XP toast restructured to hold at full opacity before fading (~3s linger), sticky back button on the Guided Path, a one-line "Nova will have a new word ready for you tomorrow!" on path completion, Grown-Ups hold gate shortened 3000ms → 1800ms. Word Builder got a static (silent, visual-only) first-letter hint after a first wrong tap; a full hint-affordance audit across every activity is in the report (Word Hunt/RhymeTime gained an audio-replay button that was missing). Say It with Nova fully migrated off `gameTheme.js` (the last standalone reader) onto Candy tokens, gained a real 5s no-speech timeout, a pronunciation-help replay button, and permission-aware auto-listen with the manual mic kept as the unconditional fallback (auto-listen may not satisfy iOS's user-gesture requirement — flagged for the device session); a real stale-recognition-callback race was found and fixed (regression-tested) while investigating the "random celebration after word 6" reports, though that exact real-device symptom itself was never reproduced. Draw It now writes a `magic_moments` row on word completion (`tracing` kind, migration `0031` approved and pushed) so the Parent Portal Moments feed shows "Traced cat!" with the real WordArt thumbnail. `test@yahoo.com` deleted with explicit approval, zero orphan rows confirmed. Formal overlap probes added for Find the Word (4Q, 0 overlaps) and Quiz Boss (8 taps, 0 overlaps) against production real audio. `gameTheme.js` census found the remaining consumers (`SoundMatch`/`SpellItOut`/`GameTypeSelector`/`UpgradeModal`) are only reachable through the orphaned `/app-legacy` route (zero live links to it) — reported rather than deleted given the blast radius; also found `story_time` still renders through the legacy dark chrome (a real live seam, not just cruft) — flagged for a dedicated small fix rather than rushed here. **Remaining (device-verifiable only, NOT part of this automatable half):** real mic behavior on a real phone, real-device celebration-misfire repro attempts, dad-testing Quiz Boss/Find the Word art, a skim of the 200 look-alike triples, and the Chrome saved-password cleanup — all scripted step-by-step in `docs/DEVICE_TEST_CHECKLIST.md` for Sal's phone session.

---

## OPEN ITEMS / NEEDS SAL (carry forward — all gate real-money launch)
- **Key rotation (STILL OPEN, mandatory before Stripe live):** live Stripe + ElevenLabs keys were exposed in chat during Phase 2; rotation unconfirmed. App on test keys so no charge risk today. See `SECURITY_CHECKLIST_FOR_SAL.md`.
- **Supabase dashboard:** password min length 6→8, enable HIBP + CAPTCHA.
- **Spend alerts:** Anthropic, ElevenLabs, Vercel.
- **CSP:** do NOT flip Report-Only → enforcing until a walkthrough confirms zero violations (audio blob: fix already landed).
- **Legal review:** COPPA inventory + draft /privacy + /terms before Stripe live.
- **Reports discipline:** require every Claude Code run to create its report file as step 0 and update it live (a recent run produced none).

## LAUNCH SPRINT (post-repair, begun with Prompt 8)
1. **Placement Adventure.** *Status: DONE (merged), see `docs/PLACEMENT_ADVENTURE_REPORT.md`.* Optional, adventure-framed placement flow at child creation — true beginners never see a probe (parent picks "start at the beginning" as the visually-default path). Deterministic 8-rung ladder (units 1/3/5/7/9/12/15/18, 2 probe words/rung, pass 2/2, 1/2 tiebreak decides), server-adjudicated via a signed stateless ladder-state token (not a DB row — placement is non-resumable by design). New `child_profiles.placement_unit`/`placement_completed_at` columns (column-level `REVOKE UPDATE` for `authenticated`/`anon` — RLS alone would let a parent's own client self-declare a floor; this closes that independent of RLS) act as a FLOOR on both the server's and client's "current unit" derivation — never fabricates `word_progress`. Free plan: floor capped to `min(measured, 5)`; the true measured level surfaces only in the Parent Portal Dashboard as an upgrade hook, never at the child. New `product_events` table (service-role-only, same pattern as `security_events`) holds `placement_started`/`completed`/`skipped`/`retaken` — the home Prompt 9's broader analytics pass should extend. Measurement exception (no errorless scaffold, miss indistinguishable from hit in tone) documented in `DESIGN_BRIEF.md` §5a as a sanctioned, onboarding-only carve-out. `idor-proof.mjs` extended to **10 checks** (forged ladder-state finalization + direct column-write attempt, both rejected).
2. **Launch Analytics + Story Time chrome + placement true-level fix.** *Status: DONE, see `docs/LAUNCH_ANALYTICS_REPORT.md`.* First-party-only launch metrics (derive-don't-duplicate: signups/children/activation/D1-D7/streaks/subscriptions all come from existing tables via `scripts/analytics-report.mjs`, read-only by construction; only `paywall_viewed` and `checkout_started` were genuinely underivable, so those became the only 2 new `product_events` types, plus a strict-allowlist `/api/track` endpoint for the client-originated one). `child_profiles.measured_unit` (migration 0034) fixes the "sharp edge" Prompt 8 flagged: a free child's Dashboard upgrade banner now reads the TRUE measured unit instead of the already-floored `placement_unit`. `subscriptions.created_at` added (backfilled from `updated_at`) — a real gap found verifying "new subscriptions by day" was actually derivable (it wasn't; the table is a single upserted row per user with no history). Story Time migrated onto the shared Candy `isE2Activity` chrome (skyGradient + top bar) and its redundant internal close button removed, in the safe 3-step order (verify shared close already threads to `handleExitEarly` → flip chrome → remove internal button), regression-protected by a new Playwright spec. `idor-proof.mjs` extended to **16 checks** (5 new: `/api/track`'s disallowed event/payload rejected, forged-identity write still lands under the real caller, unauthenticated rejected) — see that report for why the historical "10/10" figure undercounted real `check()` calls by one.

## BACKLOG (post-repair)
Sparks spend path + Nova Growth Mirror · Session Complete screen redesign (A2: Nova glow + XP/Sparks + words-learned progress + growth-mindset copy + real child name + WordArt chips) · SM-2 Star Keeper · WordArt Batch 3 (Units 11–18, mostly SKIP/function) · referral/gift/email digest/certificates · parent access codes · Educator Loop (teacher portal v2, Live Classroom, SSO) · PWA/offline · accessibility · staging Supabase project · **delete `/app-legacy`** (Prompt 7 census, re-confirmed Prompt 9: the entire legacy `App.jsx` tree — `SoundMatch`/`SpellItOut`/`GameTypeSelector`/`UpgradeModal`/`GAME_TYPES` — has zero live links anywhere in the real app; needs its own explicitly-scoped removal pass, not a footnote. Prompt 9's `gameTheme.js` (`T`) re-census found `SessionProgress` becomes dead alongside these once `/app-legacy` goes, but **`SessionComplete` does not** — it's live, imported directly by `PlayScreen.jsx`, and would need its own migration off `T` separately).

## COMPLETION ESTIMATE (vs. Product Blueprint)
Child Loop ~85% · Parent Loop ~78% · Educator Loop ~10% · Institution 0%. **Launchable paid consumer product: ~85%.** Placement Adventure and launch analytics are both done. Remaining gap to launch: device-verified items (real phone/tablet walks haven't been done for placement or the new Story Time chrome), Stripe live + legal review + key rotation, and the final launch sweep.

**Standing constraint (Prompt 9, applies to all future work):** since migration 0033, `authenticated`/`anon` have zero UPDATE privilege on `child_profiles` at the table level (not just placement's two columns) — every future `child_profiles` write, including any avatar/name-edit feature, must go through a service-role server endpoint, or it will fail with an opaque permission error that looks like an RLS bug but isn't.

## HARD-WON SESSION RULES
- Reproduce live BEFORE fixing; triage verdicts before code (a prior triage found 6/9 "bugs" were ghosts in the legacy tree).
- Branch → commit per unit → build/check:no-emoji per commit → full gates → preview verify with fresh test account → merge → production verify → clean up test accounts (`nextgenprecisiondrones+*@gmail.com`).
- Re-run idor-proof whenever queries touching progress/words/subscriptions change.
- Verify the WHOLE rendered screen, not just the changed element (the distractor-emoji bug slipped through a too-narrow check).
- Every run writes its report file as step 0, **including a RUN TIMING line (start timestamp; end timestamp + total wall-clock at close-out)** — standing rule as of Prompt 8.
- No phonics, ever (see Core Methodology Rule).
- Update this doc at the end of major sessions.

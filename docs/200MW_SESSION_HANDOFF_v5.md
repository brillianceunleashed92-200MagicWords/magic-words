# 200 MAGIC WORDS — SESSION HANDOFF v5 (start of new chat)
**Written:** July 7, 2026 · Attach this + `200MW_Master_Project_Doc_v4.md` (v5 pending — see FIRST ACTION) to any new conversation. **Supersedes v4.**

## HOW WE WORK (proven over ~22 prompts — do not change)
- **Claude in chat** = architect/reviewer: reviews attached run reports, writes the next prompt as a self-contained `.md`, catches what runs miss. This session's catches: a phantom prompt file (Package A claimed written, never existed), two runs leaving the docs push uncertified, a `/goal` false-positive that fired "achieved" mid-Phase-4, an environmental-misdiagnosis that nearly shipped a crashing app, and a growing provenance gap (undocumented commits/migrations on main).
- **Local Claude Code CLI** = executor: drop the prompt into `docs/`, launch `claude` inside `~/magic-words`, kick off with "Execute docs/<file>.md end to end. Commit the prompt doc itself as part of the run." Long autonomous runs use `/goal` with a STOP-valid completion condition (see the `/goal` template note below).
- Reports come back as FILE ATTACHMENTS. Every run: report at STEP 0 with RUN TIMING, live updates, full gates, merge → approval → push → deployment check → production verify → cleanup → docs push, **self-certified as FINAL STATUS's last line**.
- Conventions in force: gated phases (`FLIP: YES`/`GOOGLE: YES`), deployment check (~3 min, never silent past 5), diagnose-then-fix (a no-fix run with evidence is a successful run), pre-approved content-edit tables, string-level bundle verification for prod content, **every run's report carries a TRAPS section** (Sal's standing request — bugs get documented so they're not re-encountered).
- Approval stops: `git push origin main`, `supabase db push`, Management-API PATCHes, Vercel prod env changes, destructive ops (incl. batch test-account deletes), live payment mode. Secrets in gitignored `.env.local`, never echoed.
- One workstream per chat. "Already written/exists" claims get verified against the repo before executing.
- **`/goal` template lesson (new)**: the completion condition must (a) name the exact terminal artifact state incl. "paste FINAL STATUS into output," (b) make a documented STOP a VALID completion, never worked around, and (c) **name the full-suite pass at its real count as an explicit gate** — this session a goal fired "achieved" with Phase 4 still open because the suite-pass wasn't a hard-coded terminal condition. Include a turn cap.

## STATE (all merged + live unless noted)
Everything in Master Doc v4's CURRENT STATE, plus this session (July 7):
- **Package A — Parent metrics dashboard (SHA 2afd849)**: 6 Progress charts live. Reviewed, closed.
- **Package B — Pedagogy calibration (merged 633d68e)**: isRealMastery moved to all readers (stored formula untouched — replay proof preserved); weeklyStats same-screen fix; scaffold-down v1 (two-completed-errors → pin easiest tier → one-correct-resets, `scaffold_down` telemetry); attempt_number wired (reuses the awaited mutation's returned attempt_count); chart-1 84-day truncation guard. Recovered cleanly from a mid-Phase-8 kill (Chrome contention). Migration **0035** added `scaffold_down` to the product_events CHECK constraint (was silently bouncing every write behind a fire-and-forget 200 — caught only by running idor-proof against a live preview). New build gate `check-mastery-predicate-sync.mjs`. Reviewed, closed.
- **FIX_CELEBRATION_R1 (executed, report NOT yet reviewed in chat)**: star-ignition-between-answers fix. **Its report is the one missing artifact — see FIRST ACTION.** It also ran the approved orphan-account cleanup and a provenance-reconciliation phase for the undocumented work below.
- **Package C — Placement report + Star Check-In (merged b865400, + post-merge 11c883b)**: parent-facing Placement Report card, eligibility-gated 30-day Star Check-In re-probe (reuses placement ladder via distinct `'checkin-ladder-v1'` signing context; `CheckInScreen` reuses `PlacementProbe` unmodified → §5a correct by construction), measured-level growth chart joining Package A's charts. `historyMode`/`checkinMode` added to session-generator (no new endpoint file). Never-regress rule: `appliedMeasured = max(raw, prior)` to floor columns, raw logged for the growth line. Migration **0036** extended the product_events CHECK constraint (`checkin_started`/`checkin_completed`). idor-proof now **28/28** (added cross-mode replay rejection + positive twins). Reviewed, closed. **Real baseline is now 75 specs** (was mis-stated as 65; true pre-C baseline was 69).

## THE PROVENANCE GAP (resolve before Master Doc v5)
Master Doc v4 is stale and v5 is NOT yet written, deliberately: the recovery audit + this session surfaced work on `main` with no report on record — migrations **0032/0033/0034** (0034 = `launch_analytics`: `paywall_viewed`/`checkout_started` events), a `story-time-chrome` branch/spec, and `mwstorytime*` test accounts created July 7 17:10/18:13 (postdating runs). Some `product_events` types (`paywall_viewed`, `checkout_started`) exist with no run explaining them. **FIX_CELEBRATION_R1's Phase 0.5 was written to reconcile exactly this** — its report should contain the table. v5 waits on that reconciliation so it isn't written against a half-guessed codebase.

## IN FLIGHT
Nothing mid-execution. Two prompts written and ready if not yet run/reviewed: none pending beyond the celebration report review.

## OPEN — SAL-GATED (launch critical path, unchanged in shape)
1. **Counsel email** — still unconfirmed as sent. Longest external clock; every new chat asks once.
2. **Accounts (~40 min)** — rotate exposed Stripe/ElevenLabs/Supabase service-role keys; hCaptcha keys → `FLIP: YES`; Google OAuth creds → `GOOGLE: YES`; spend alerts.
3. **Virtual mailbox + business phone** → unblocks legal-pages ship + counsel package.
4. **Phone/device session** — `DEVICE_TEST_CHECKLIST.md`; iOS Say-It auto-listen is the #1 open product question.
5. **Prompt 11 — Stripe-live cutover**: written the moment rotation + counsel are green.

## NEXT WORK (order)
- **FEAT_BLANK_ENGINE_R1** (written next, not yet executed): Dr. Blank's exposure rule as a real engine change — function-word track EXEMPT from the placement-floor skip (all children cycle function words in context regardless of level, per Blank); genuinely-mastered content words frequency-DAMPED in the session mix (Star Keeper does most; damp the mix + confirm placement doesn't reintroduce). PLUS the sixth-skill gap: **comprehension check after Story Time** (1–2 errorless picture questions — today nothing verifies understanding after a story).
- **WEEKLY_INSIGHTS (capstone, self-improving loop)**: a scheduled/Monday `/goal`-wrapped run that reads the week's `learning_events`/`product_events`, produces ONE report (activities by mastery-speed, scaffold-down clusters, look-alike triples with outlier error rates, drop-off points, check-in growth), each finding paired with a PRE-APPROVED edit table. Sal's input collapses to yes/no per row; approved rows become the next quick-win run. **Standing principle: data proposes, Sal ratifies — the curriculum never self-modifies silently.** Sharper once check-in growth data exists, so it lands after Blank engine.
- **Package E — Quick wins**: unit-relative Session Complete framing, sleeping stars, honest "Minutes this week" label, streak mercy, Grown-Ups "Settings" 375px clip, **+ the password-reset `verifyOtp` recovery-session flake** (confirmed on unmodified main this session — its own small fix).
- **Package D — Admin panel v1**: separate private Vercel project, read-only first. Reuse `historyMode`'s "ownership-verified minimal-projection read into a service-role-only table" pattern (C's Notes-for-D).

## TRAPS ADDED THIS SESSION (full list → Master Doc v5 once provenance clears)
- **A uniform "everything times out" suite failure is NOT proof of an environmental cause** — capture browser console/pageerror directly (5-line Playwright script) before blaming memory/rate-limits. This session burned an hour misdiagnosing a dead-import crash as memory pressure.
- **Vite dev throws hard on a missing named export; Rollup `vite build` silently tolerates it** — a stale `import {x}` after `x` was refactored out passes build + every preview/prod walk clean but crashes the whole app in dev/Playwright. Clean dead imports on refactor immediately.
- **A negative security check needs a positive twin** — an empty result set satisfies "forged id ≠ written id" vacuously; pair every rejection check with a demonstrated legitimate landing. (Caught B's constraint bug; applied throughout C.)
- **A deploy-gated idor check that only ever ran in the `if(deployBase)` SKIP branch has never actually executed** — run extended checks against the live preview before counting them green.
- **New `product_events` types → DB CHECK constraint AND (if client-posted) the `api/track` allowlist, same change, + a positive-landing test** — migration 0035's whole reason. Server-only-logged types don't touch the allowlist (documented), but the CHECK constraint is non-negotiable.
- **Shared signed-token helper → distinct per-flow signing context string** (default-valued so existing callers don't break) — closes cross-mode replay for free.
- **Multi-mode endpoint rate limits are shared per-user across all modes** — a verify script running many negatives then a real multi-step flow on one identity burns its own budget → false 429. Spend a second identity's budget for the positive flow.
- **Bounded sub-ladder `finalize` sentinel must check the walk's own lower bound, not literal 0** — else reads outside the tested range and reports an unverified unit.
- **`child_profiles` REVOKE UPDATE blocks authenticated/anon only, never service_role** — seed otherwise-unwritable columns directly via the admin client in fixtures.
- **No unbounded background waits** — if a suite exceeds ~2× baseline, interrupt, identify the executing spec, triage (this session a 33-min hang; the deployment-check "never past 5 min" rule needs a suite-runtime sibling).
- **`/goal` conditions must name the full-suite pass at real count as an explicit terminal gate** — else the evaluator fires "achieved" on visible-green-checks with a phase still open.

## PROJECT-KNOWLEDGE HYGIENE (Sal, in the Claude UI)
Keep: **Master Doc v5 (once written) + this handoff (v5)**, `200MW_APP_ANALYSIS_SUMMARY.md`, the newest reports (`PLACEMENT_CHECKIN_REPORT.md`, `CELEBRATION_FIX_REPORT.md` once reviewed, `PEDAGOGY_CALIBRATION_REPORT.md`), `DEVICE_TEST_CHECKLIST.md` + results, the `_PUBLISH` policy set, `DESIGN_BRIEF.md`. Remove: Master v3/v4, handoffs v3/v4, older executed prompt files and run reports — everything lives in repo `docs/`.

## FIRST ACTION OF THE NEW CHAT
**Attach `docs/CELEBRATION_FIX_REPORT.md`** (executed but never reviewed in chat) — it holds the Phase 0.5 provenance table that unblocks Master Doc v5. Also state, with real values: counsel-email status, gate tokens ready (`FLIP`/`GOOGLE`/mailbox), rotation status, and which workstream this chat owns (review the celebration report + write Master Doc v5 · FEAT_BLANK_ENGINE_R1 execution review · Prompt 11 the moment rotation + counsel are green · WEEKLY_INSIGHTS spec). Claude verifies any "already exists" claim against the repo, reviews what's attached, and picks up there.

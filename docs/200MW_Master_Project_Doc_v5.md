# 200 Magic Words — Master Project Document (v5)
**Last updated:** July 13, 2026 (refreshed by DOCS_MASTER_V5_R2) · Production LIVE at https://200magicwordsapp.com
**Refreshed after DOCS_MASTER_V5_R2** (`docs/DOCS_MASTER_V5_R2_REPORT.md`) — a report claimed this artifact was absent from the working tree; forensics found it was never actually lost from `main` (it was committed and merged the same day R1 ran). The false-negative traced to `feat/quick-wins` — a branch parked since before this file existed — being the branch checked out in the primary worktree at the time of the check; `main`'s checkout lives in a separate worktree. Full evidence chain in the R2 report. This refresh updates every section to current `HEAD` (`117327b` + this run's own docs commits) regardless — items 22-23 below are new since R1, and every number in FRESH CENSUS was re-measured this session.
**Supersedes v4** (`docs/200MW_Master_Project_Doc_v4.md`, dated July 7 — kept in place, header-marked superseded, not deleted). v4 itself was never committed to the repo before R1's run (found only in a local Downloads staging area); it was committed alongside v5 by R1, purely as historical record.

**Stack:** React 19/Vite · Supabase (`ozhqsaysltiamadpcruz`) · Vercel · ElevenLabs · Claude API · Stripe (TEST mode) · Recharts 3.9.2 (parent dashboard, lazy chunk only)
**Repo:** brillianceunleashed92-200MagicWords/magic-words · SSH push · Vercel auto-deploy
**Workflow:** launch `claude` inside `~/magic-words` (never `cd`). `.claude/settings.local.json` allowlist + PreToolUse auto-approve hook; approval stops: `git push origin main`, `supabase db push`, Management-API PATCHes, Vercel prod env changes, destructive ops, live payment mode. Helpers: `scripts/db-query.mjs` (reads), `scripts/admin-user.mjs` (test accounts — **must include `parental_consent` metadata or fixtures hang on the COPPA interstitial**), `scripts/idor-proof.mjs`.
**Conventions (unchanged since v4, still load-bearing):** every run writes its report file at STEP 0 with RUN TIMING and updates it live; **FINAL STATUS's last line self-certifies the post-production-walk docs push**; gated phases stop before key-dependent steps until the kickoff carries the token (`FLIP: YES`, `GOOGLE: YES`); deployment check after every push (GitHub commit-status API + `vercel list`/`vercel inspect` — **not** the Vercel MCP, which is authed to the wrong account); diagnose-then-fix (no fix until a reproduction demonstrates the layer — a no-fix run with evidence is a successful run); content edits use pre-approved tables with fallbacks; production content changes get string-level bundle verification. **New this cycle**: git worktrees (`.claude/worktrees/<name>`) are now the default isolation mechanism for parallel/sequential runs — each run gets its own branch + working tree so it never collides with another session's uncommitted work on the primary checkout.

## WHAT THIS APP IS
AI-powered early literacy app (ages 4–8) built on **Dr. Marion Blank's methodology**. 200 foundational words across 18 units, adaptive games, ElevenLabs audio, Candy Galaxy theme, Nova the Comet Spark. Freemium: Units 1–5 free, 6–18 on Family Plan ($9.99/mo / $79/yr — Stripe checkout built + verified, still TEST mode).

### CORE METHODOLOGY RULE (non-negotiable, every prompt)
Dr. Blank is **anti-phonics**. NEVER sound-blend / sound out anywhere ("cuh-a-tuh" confuses early readers). Words are whole units tied to meaning: Find the Word, cloze-in-context, repeated meaningful exposure, whole-word recognition. Struggle is met by scaffolding DOWN the demand, never by drilling phonemes. **This cycle's sharpest reminder of the stakes**: a brand-new child's freeform-generated story ("Aliya cat.", "Cat cat.", "Aliya cats.") both violated whole-word-only teaching (an inflected "cats" that isn't one of the 200 words) *and* was simply not language — see Story Engine below.

## CURRENT STATE — MERGED & LIVE
Items 1–13 are carried forward from v4 (re-verified where this run touched them; unchanged claims not re-litigated). Items 14+ are new this cycle.

1. **Candy Galaxy v2 architecture** — componentized, lazy routes, legacy monolith parked at `/app-legacy` (zero live links; deletion is its own backlog pass).
2. **Guided Path** — vertical journey per focus word, activities valid for the word type, pedagogical order via the difficulty governor.
3. **Phase 2 Parent Loop** — multi-child profiles, Story Engine, 4-tab Parent Portal, time limits, all-200-word ElevenLabs audio.
4. **Security hardening (8 phases)** — RLS audit/fixes, JWT + per-user rate limits on all AI/TTS endpoints, TTS caching, CSP (Report-Only), COPPA baseline, cascade deletion, consent checkbox.
5. **Word-list unification** — server-authoritative session-generator off the real 200-word `words` table; free-tier Units 1–5 cap server-side; `word_type` column.
6. **WordArt system** — `words.has_art` + manifest + build-gated sync; no letterforms in art; Nova verb set for all 9 standalone verbs.
7. **Completion/celebration pipeline** — per-question `onProgress` → `learning_events` writes → `onSessionEnd` awaits settled promises → completion check. `isRealMastery` (≥80 AND ≥3 attempts) gates tier-2 celebration. **Timing bug fixed this cycle** — see item 15.
8. **Activity roster (canonical 10)** — `word_match`, `word_hunt`, `rhyme_time`, `find_the_word`, `flash_cards`, `story_time`, `story_builder`, `word_builder`, `say_it`, `draw_it`. `SCORELESS_GAME_TYPES` = `draw_it`, `word_builder`.
9. **Placement Adventure** — optional adventure-framed placement at child creation; signed stateless ladder token; `placement_unit` floor; free plan floor capped at min(measured, 5); `product_events` table (service-role-only). **Extended this cycle** — see item 17 (Star Check-In reuses the same ladder machinery under a distinct signing context).
10. **Auth/security sprint** — PKCE; password reset LIVE; signup clickwrap; COPPA consent interstitial; hCaptcha/Google sign-in built INERT (gated); plan cache child-scoped; `product_events` purged on account deletion. Legal pages rendered + wired on `chore/legal-pages`, **still not shipped** — blocked on business mailing address + phone (unchanged since v4).
11. **CONTENT_R1** — Find-the-Word manifest cleaned, string-verified in the prod bundle.
12. **Quest-fix disposition** — Sal's "frog / 0 of 10" report did not reproduce; no behavioral change shipped (correctly); `check-activitydefs-sync.mjs` + `tests/quest-progression.spec.js` shipped instead.
13. **Package A — Parent metrics dashboard** — 6 Progress charts (weekly mastery crossings, practice heatmap, accuracy by activity, weekly-median answer speed, review-due forecast, unit progress). `src/lib/masteryCalibration.js` established as THE calibration seam — every later workstream (14, 16, 17) mirrors or extends this same module rather than re-deriving mastery logic.

14. **QA_HARDENING_R1 — read-only end-to-end QA sweep** (`qa/e2e-audit` branch, **not merged to main** — artifact-only run, its report lives only on that branch). Verdict: app fundamentally solid; 1 P0 + 2 P1 + 3 P2 + 3 P3 (9 issues) found, zero fixed in that run by design. **Of the 9, 2 are now fixed** (QA-03, QA-04 — see item 19); **7 remain open** — see OPEN ITEMS for the live status table. Also confirmed-not-a-bug: the pre-flagged "known Grown-Ups Settings 375px clip" was a `fullPage:true` Playwright screenshot artifact (a `position:fixed` nav smeared across stitched scroll frames), not a real overlap — logged in TRAPS so it isn't re-flagged.

15. **FIX_CELEBRATION_R1 — star-ignition timing fix** (merged `7e355dc`). Root cause: `GameEngine.handleAnswer` fired `onProgress` (→ `PlayScreen.handleProgress`, which awaits a real Supabase write before queueing the celebration) **without awaiting it** before proceeding to `onSessionEnd` on the session's last question — the orphaned promise could resolve after the screen already swapped to `SessionComplete`, landing the celebration on whatever screen the global `CelebrationRenderer` overlay happened to catch next (worst case: a bare "Loading your galaxy…" screen with zero chrome). Fixed by awaiting that promise before ending the session (`GameEngine.jsx`), a `sessionEndedRef` backstop gate on `queueCelebration` (`PlayScreen.jsx`), and a new client-side `src/lib/celebrationDedup.js` (localStorage-keyed per child/word/day — cumulative mastery isn't a one-way ratchet, so a same-day re-cross needs its own dedup, not a migration). Verified: repro gap 2815ms → 0ms; production walk showed ignition ~1839ms after the answer, anchored inside the still-active activity.

16. **FEAT_PEDAGOGY_CALIBRATION_R1 — mastery gate + scaffold-down v1 + attempt wiring** (merged `633d68e`). Three independent pieces:
    - **Mastery gate**: `isRealMastery` (already established by Package A) rolled out to 14 remaining consumers that still used a naked `mastery >= 80` check (client screens + `api/session-generator.js`). Stored formula, raw `percent`, and unit-number gates untouched. Server side is CommonJS and can't import the ES-module constants, so `api/session-generator.js` mirrors them; `scripts/check-mastery-predicate-sync.mjs` (wired into `npm run build`) asserts numeric identity so the mirror can't silently drift.
    - **Scaffold-down v1**: two completed errors on the same word (tracked session-locally in `PlayScreen.jsx`) pins that word's Guided Path node to a derived easier activity (first entry of the word's existing `getEligibleActivities` ranking — `word_match` for has-art content words, `rhyme_time`/`find_the_word` otherwise) and shows a persistent non-punitive banner. A correct completion at the pinned activity clears the pin. Logs `scaffold_down` to `product_events` (`api/track.js`).
    - **attempt_number wiring**: `learning_events.attempt_number` existed in schema but every insert hardcoded `1`; now writes the real post-write attempt count from `useSaveWordProgressMutation`'s own return value. **Forward-only — historical rows before this deploy keep literal `1`.**
    - Also folded in: the `weeklyStats.js` same-screen fix (both derivations now threshold/attempt-gated, matching chart 1) and a chart-1 84-day-window truncation guard (`computeWeeklyMasteryCrossings` now discards a crossing whose replay-final counts don't match the word's real all-time stored counts, rather than risk a false "fresh crossing" off a fetch-window edge).
    - **Hard-won lesson, load-bearing for every future `product_events` type**: adding a new event name to `api/track.js`'s client allowlist is not sufficient — the `product_events_event_type_check` CHECK constraint (a migration) must be updated in the *same* change, or inserts silently fail (fire-and-forget, `console.error` only, endpoint still returns 200). This bit `scaffold_down` itself (zero rows landed for a while, caught by `idor-proof.mjs`), and is exactly the same class of gap this cycle's Story Engine telemetry deliberately avoided (item 21) by reusing an already-correctly-wired event type instead of adding a new one ungated.

17. **FEAT_PLACEMENT_CHECKIN_R1 — Placement Report + Star Check-In + growth chart** (merged `b865400`). `PlacementReportCard.jsx` (Dashboard tab) shows placement state + absorbs the pre-existing true-level upsell banner into one card. `StarCheckInCard.jsx` gates on `daysSince(placement_completed_at) >= 30` (measured, not recalled: `src/lib/checkinEligibility.js`), launching `CheckInScreen.jsx` — built on `PlacementAdventureScreen`'s shape and reusing `PlacementProbe.jsx` **unmodified**, which is what guarantees the §5a errorless-carve-out tone parity by construction rather than by re-implementation. Server-side `checkinMode` on `api/session-generator.js` reuses `placementLadder.js`'s `RUNGS` verbatim under a *distinct* signing context (`'checkin-ladder-v1'` vs. placement's `'placement-ladder-v1'`) so tokens can't replay cross-mode. Never-regress: `appliedMeasured = Math.max(rawMeasured, storedMeasuredUnit)` is what's stored; the **raw** value is what's logged to `product_events` and plotted on the new `GrowthChart`. New event types `checkin_started`/`checkin_completed` — server-only (like placement's own events), no `api/track.js` allowlist change needed. Migration 0036 extended the CHECK constraint.

18. **FEAT_BLANK_ENGINE_R1 — function-word universality + mastered-content damping + story comprehension as sixth skill** (merged `04e374b`). Three pieces, all in `api/session-generator.js`'s `selectCandidateWords` / `src/lib/blankEngineWeighting.js`:
    - **Function-word universality**: below-floor unmastered function words previously had no path into the candidate pool at all (a floor that only ever rises means a word below it never gets exposure). New `belowFloorFunctionEligible` filter samples 1 in per call. **Real, disclosed gap, not fixed here**: all 45 function words live in units 11–18, and free-tier accounts never load past unit 5 — this exemption is a real fix only for **family-plan** accounts. Free-tier function-word exposure is a genuine product gap, explicitly flagged to Sal rather than silently patched.
    - **Mastered-content damping**: the pre-existing "1–2 mastered words for confidence" sample now applies `MASTERED_CONTENT_INCLUSION_WEIGHT = 0.35` to mastered *content* words only (mastered function words still always pass at weight 1.0). Spaced-repetition review (`dueForReview`) is untouched — damping only affects the confidence-sample slot. Both constants are explicitly flagged in-code as tunable v1, pending real accuracy/engagement data.
    - **Story comprehension as sixth skill** — **the same mechanic Story Time already used**, not a new one: `story_catalog.comprehension_question` had existed since migration 0030 with 20 real seeded questions. The actual gap was UI-only: `StoryReader.jsx` now uses the same `AnswerTile`/`WordArt` picture-choice primitives and the same wiggle-soften → hint-glow → second-miss-completes errorless state machine every other multiple-choice activity uses, instead of plain one-shot text buttons. Scoring contract, `game_type`, and `product_events` types are all unchanged. `StoryScreen.jsx` (freeform Story Engine) never sets a comprehension question — confirmed unchanged; this upgrade only touches Story Time.
    - A real bug (pool-array ordering starving both new samples *and* the pre-existing mastered-sample) was found live during the Phase 6 preview walk and fixed before merge — see TRAPS.

19. **FIX_NO_BLANK_SCREENS_R1 — mount ErrorBoundary + add 404 route** (merged `4b2dfd0`, fixes QA-03/QA-04 from item 14). `ErrorBoundary.jsx` already existed (153 lines, well-designed) but was never imported anywhere; now wraps the whole router in `main.jsx`. New `src/pages/NotFound.jsx` (own lazy chunk) added as a catch-all `<Route path="*">`. Internals of `ErrorBoundary` deliberately untouched — mount-only fix. Verified against a real production build (`vite preview`): a forced render error shows the friendly "Cosmo needs a moment!" recovery UI with zero dev-only error detail leaking. **Known limitation, explicitly out of scope**: a module-load-time throw (e.g. missing `.env.local` in a fresh worktree) happens before React mounts and `ErrorBoundary` structurally cannot catch it — a genuinely blank page is still possible in that one narrow case.

20. **PERF_ACTIVITY_LOAD_R1 — tap-to-playable AI-call latency** (merged `93bf99c`). Root cause: cold-tap latency (~7.1s median) is ~92–95% `/api/session-generator`'s own server time, and within that endpoint the blocking Anthropic call for flavor text (not word/activity *selection*, which is 100% deterministic and fully computed before the AI call ever fires) is the dominant cost. Three selection-neutral fixes shipped: (a) client-side cached-plan reuse on a focus-word tap (`reorderPlanForFocusWord` — a real pre-existing double-fetch bug fixed: every word tap fired two calls to `generatePlan`, discarding a valid cache even when one existed); (b) Home-mount prefetch (`prefetchSessionPlan`, restoring the session-plan hook's own documented "generate once at login" intent, which had drifted since the hook only ever lived inside `PlayScreen`); (c) server-side parallelization of `fetchChildContext`'s two independent reads. **Net effect**: cold tap is deliberately unchanged (~7-12s, noisy, AI-call-bound); a warm/prefetch-covered tap dropped from firing the AI call *at the tap* to firing it *during the preceding dwell* — production-verified `wordTapToPlayableMs` 5766ms with the session-generator call already in flight before the tap, not after. **Deliberately left untouched, per Sal's explicit instruction**: the blocking Anthropic call itself and its AI-chosen `sessionLength`. Three named options logged for a future pass (always use the deterministic `defaultLength` fallback; cap the AI call with a timeout degrading to that fallback; proactively regenerate the plan in the background post-session) — plus a separate, explicitly-flagged-as-pedagogy-not-perf open question: should AI-chosen `sessionLength` exist at all vs. a fixed per-difficulty number.

21. **FIX_STORY_QUALITY_R1 + FIX_STORY_FOLLOWUP_R1 — the Story Engine no longer produces non-language** (merged `9115eb0`, then `eac7a0b`). A brand-new child (pool size 1, the *normal* first-session state under the mastery-gate floor, not an edge case) received a degenerate freeform-Story-Engine story ("Aliya cat.", "Cat cat.") while a hand-authored `story_catalog` story for the same word sat unreachable from that surface. Reproduced live on production first: the AI call did **not** fail — it succeeded legitimately on a retry against an artificially 1-word vocabulary pool, and an inflection-permission rule (`stripsToAllowed`, "cats" counts as valid given "cat") let a non-200-word token through. Fixed in two passes:
    - **Quality floor + routing** (`FIX_STORY_QUALITY_R1`): `MIN_MASTERED_WORDS_FOR_GENERATION = 4` (`src/lib/queries/stories.js`) — below this, `StoryScreen.jsx` never calls the AI at all, instead serving `story_catalog` content (new `findCatalogStoryForWord`, tries the child's real tier then falls through 3→2→1, since production catalog coverage today is 100% tier-3) or a small guaranteed-safe local template as last resort. `api/story-engine.js`'s `validateStory` now requires exact word-list membership — no inflection loophole — for the AI path.
    - **Follow-up** (`FIX_STORY_FOLLOWUP_R1`, Sal-decided): the first pass's catalog-vocabulary gate was removed — curated content's richer read-aloud vocabulary ("yard", "paw", "nap" in "The Curious Cat") is the methodology, not a violation; that strictness stays only on the AI path. A brand-new child targeting "cat" now receives **"The Curious Cat" verbatim** from `story_catalog`. Also replaced a `scaffold_down` telemetry reuse (added by the first pass) with a plain `console.warn` — that write was polluting the pedagogically meaningful signal item 16 established; a proper fix needs a new `product_events` type, deliberately deferred to a migration (see OPEN ITEMS).
    - **A real, separate, unresolved finding surfaced while building this fix's own test**: `story_catalog` content itself is not guaranteed to stay inside the 200-word list (confirmed word-by-word against the real `words` table, not assumed) — see OPEN ITEMS.

22. **FIX_PARENT_SURFACE_R1 + follow-up — the Parent Dashboard no longer tells a truthful-looking lie** (merged `489d959`, then `97a16f4`). Resolves the "parent-surface blindness" item flagged as open by v5's own prior revision. The AI Insight and the two stat cards it draws its framing from were 100% `learning_events`-derived, so a child who'd just been placed and read a full story still got told they "hasn't jumped into the app yet this week" — reproduced live on production with a disposable account before any fix. Fixed read-side only (no migration, no schema change, no `api/session-generator.js` touch): fed the AI Insight two true facts it was blind to (`placement_completed_at` within 7 days, `stories.read_at` count within 7 days). Words-this-week/streak/minutes stat cards deliberately left untouched (each verified to already be correct on its own terms). A follow-up pass fixed a real mislabel Sal caught in review: the insight had asserted "Star Check-In" as fact for a child's first-ever placement (client data can't distinguish placement from check-in — both write the same `placement_completed_at` column) — the prompt now instructs the model to describe the event neutrally ("completed a level check") rather than asserting either specific term. Both passes verified live on production with disposable accounts (3 total, all cleaned up); the real "Aliya" account was never touched. Full suite 98 passed/3 failed/101 total on the follow-up's corrected (env-sourced) baseline, all 3 failures independently confirmed pre-existing/flaky by direct comparison against unmodified `origin/main`, zero attributable to this diff.

23. **FIX_MIGRATION_DRIFT_R1 — main's migration history now matches what's actually applied to production** (merged `29ecc2b`, then a closing docs commit `117327b`). Migrations `0037_streak_freeze_grant_tracking.sql` and `0038_streak_freeze_events.sql` had been applied to the live production database from the parked `feat/quick-wins` branch, but `main`'s `supabase/migrations/` folder stopped at `0036` — exactly the drift this doc's own prior revision flagged as an open risk, plus a separate collision where `FIX_STORY_FOLLOWUP_R1`'s `story_fallback` TODO had independently claimed migration number `0037`. Verified production's applied state against both files first (read-only `supabase migration list` + `information_schema`/`pg_constraint` queries) — exact agreement, no disagreement, no wider drift found — then copied the two files onto `main` byte-identical (diff + blob-hash confirmed). Added `supabase/migrations/MIGRATIONS.md`: current highest applied number, the rule ("next migration number is the one after what's *applied to production*, not after what's merged on `main`" — checked via `supabase migration list`, not assumed from the file listing), and the provenance note. Moved the `story_fallback` TODO to `0039+`. **No database write of any kind occurred** — every guardrail (no `supabase db push`, `feat/quick-wins` untouched) held. **Note: this resolves the migration-*file* drift only** — `feat/quick-wins`'s own application code (the streak-freeze UI, sleeping stars) remains parked and unmerged; see WORKSTREAM STATE.

24. DESIGN_BRIEF_V2_R1 (2026-07-13, docs-only): Blank-overhaul design canon locked. Mockups F-M committed as canon (N reference-only) at docs/design/mockups/; BLANK_METHOD_SOURCES.md committed as pedagogical source-of-record; DESIGN_BRIEF_V2.md written (ladder, session economy, pretest, guided completion, mastery redefinition, unit gate, placement v2, celebration economy, component contracts, motion and sound spec); DESIGN_BRIEF v1 and mockups D/E2 superseded. No app code changed.

## FRESH CENSUS
Every number below was measured this session (`DOCS_MASTER_V5_R2`, 2026-07-13); the exact command is inline. Where an earlier report's number differs, the measured figure here is the current truth (see footnotes) — not averaged, not guessed. Full detail and raw command output: `docs/DOCS_MASTER_V5_R2_REPORT.md`, PHASE 2.

| Metric | Value | Command |
|---|---|---|
| Playwright spec files / individual tests | **34 files / 101 tests** (up from v5's original 33/95 — `parent-digest-fallback.spec.js` added 1 file/6 tests) | `npx playwright test --list` |
| Migrations on `main` | **38 files**, `0001`–`0038`, latest `0038_streak_freeze_events.sql` | `ls supabase/migrations/*.sql \| wc -l` |
| Migration drift (main's files vs. production's applied state) | **RESOLVED, none remaining.** `supabase migration list` shows Local==Remote for every version `0001`-`0038` — the `0037`/`0038` drift this doc previously flagged is fixed, see item 23 | `supabase migration list` (read-only) |
| `words` table (the 200-word source of truth) | **200 rows** — 155 content / 45 function, 18 units. Unchanged. | `select count(*), count(*) filter (where type='content'), count(*) filter (where type='function'), count(distinct unit) from words` |
| `story_catalog` rows | **20** — 20 distinct words, all tier 3 (zero tier-1/2 rows exist despite schema support). Unchanged. | `select count(*), count(distinct target_word), count(distinct tier) from story_catalog` |
| `product_events` allowed types (live DB, via the CHECK constraint) | **11**: `placement_started/completed/skipped/retaken`, `paywall_viewed`, `checkout_started`, `scaffold_down`, `checkin_started/completed`, `streak_freeze_granted/used`. Unchanged. | `select pg_get_constraintdef(oid) from pg_constraint where conname='product_events_event_type_check'` |
| `product_events` types reflected in `main`'s migrations | **11 of 11** — all now on `main` (previously 9 of 11, with the 2 `streak_freeze_*` types off-`main`; fixed by item 23) | code inspection, `supabase/migrations/0034`–`0038` |
| `api/track.js` client-side allowlist | **2** event types (`paywall_viewed`, `scaffold_down`) — the other 9 are server-only, no client allowlist entry needed | `grep -A20 EVENT_SCHEMAS api/track.js` |
| `idor-proof.mjs` checks | **29**. Unchanged. | `grep -c "check(" scripts/idor-proof.mjs` |
| Specs that hardcode/override `baseURL` to hit production directly | **5 files / 14 tests** (`csp-walk`, `overlap-probes`, `placement-adventure`, `placement-checkin`, `pedagogy-preview-walk`) — needed because `/api/*` endpoints aren't served by local `vite dev`/`preview`. Unchanged. | `grep -rl "200magicwordsapp.com" tests/*.spec.js` + `playwright test --list` on those 5 files |
| Full suite pass state | **98 passed / 3 failed / 101 total** (15.7m, `--workers=1`, env-sourced) — most recent full-suite run this session, from the `fix/migration-drift` run immediately preceding this one; see footnote | `set -a; source .env.local; set +a; npx playwright test --workers=1` |

**The full-suite figure's provenance**: this revision's own Phase 2 instructions were explicitly "count specs, don't run the suite" (see `DOCS_MASTER_V5_R2_REPORT.md`) — the 98/3/101 figure above is not a fresh run of this exact revision, it's the most recent full-suite run in this same session (`fix/migration-drift`, immediately prior, against code identical to current `HEAD` since only docs changed since), cited rather than re-run. All 3 failures (`blank-engine-comprehension.spec.js:94`, `find-the-word.spec.js:113`, `placement-checkin.spec.js:153`) were independently determined not attributable to that run's diff and are consistent with this codebase's established per-run flaky-test pattern — full analysis in `docs/MIGRATION_DRIFT_REPORT.md`'s VERIFICATION section, including a same-session **99 passed/2 failed/101 total** baseline run showing *zero overlap* in which specific tests failed between the two runs.

**Footnote on the spec-count history** (don't average, state the sequence): v4 said "baseline 60 specs." Measured sequence since: 65 (celebration-fix) → 69/75 (placement-checkin, corrected from an assumed 65) → 86 (blank-engine) → 90 (no-blank-screens) → 93 (activity-load-perf) → 95 (story-quality + story-followup) → **101 today** (parent-surface added `parent-digest-fallback.spec.js`, 6 tests). Each jump is a real, cited addition, not drift.

**Footnote on the "7 prod-locked specs" figure** in `DOCS_MASTER_V5_R1.md` and in `ACTIVITY_LOAD_PERF_REPORT.md`'s traps: measured fresh in R1's run at **5 files / 14 tests**, re-confirmed unchanged this run. Not reconciled further — could be a stale recollection, or a different counting convention; 5/14 is the current directly-measured truth.

## KEY REFERENCE
- **Tables:** `words` (200), `word_progress` (incl. `next_review_at` — Star Keeper 1/3/7/14/30 ladder), `learning_events` (incl. `response_time_ms`, `attempt_number` — **now genuinely wired**, see item 16, forward-only), `user_streaks` (incl. `streak_freeze_count`, `freeze_last_granted_at` — schema live via migration 0037, application code still parked on `feat/quick-wins`, see item 23/WORKSTREAM STATE), `user_stats`/`user_sparks`, `child_profiles` (placement columns REVOKE-protected — `service_role` can still write them, a real and useful test-fixture affordance), `parent_settings`, `subscriptions`, `api_rate_limits`, `webhook_failures`, `security_events`, `product_events` (11 allowed types, all now reflected on `main` — see census), `story_catalog` (20 rows, migration 0030), `stories` (freeform Story Engine output). **Migrations 0001–0038 all on `main`, all applied to production, zero drift** (fixed this cycle, item 23 — `supabase/migrations/MIGRATIONS.md` now documents the numbering rule: next number is the one after what's *applied*, not after what's merged). ON DELETE CASCADE verified (re-confirmed by `QA_HARDENING_REPORT.md`'s cascade audit and by every workstream's own cleanup step this cycle).
- **Mastery formula (write path, pure):** `useSaveWordProgressMutation` — cumulative `correct_count`/`attempt_count`, `mastery = round(ratio*100)`. Sole writer, unchanged. **Read-path gate** (`isRealMastery`, `src/lib/masteryCalibration.js`): `mastery >= 80 AND attempt_count >= 3` — now applied at 14+ consumer sites (item 16), server side mirrored (not imported) into `api/session-generator.js` with a build-gated sync check.
- **Stripe (TEST):** unchanged since v4 — `prod_UoN8JYjYwDK7lw`; monthly `price_1TokOI1HwJlEooq4y3crPkgC`; yearly `price_1TokOI1HwJlEooq4n13cOA46`. Identity from verified JWT.
- **AI endpoints (JWT + rate-limited):** `speak` 60/min (cached by text hash), `session-generator` 10/min (shared across plain/`reviewOnly`/`placementMode`/`checkinMode`/`historyMode` — **a verification script hitting one mode can starve another on the same identity**, see TRAPS), `ai-helper` 30/min, `story-engine` 4/day (now with a quality floor that skips the AI call entirely below 4 mastered words — see item 21), `parent-digest` 4/day.
- **Audio:** module-level singleton, one sound at a time; CSP media-src allows `blob:`.
- **Gates:** `npm run build` runs **6** sync checks before `vite build` — wordart-sync, stroke-coverage, findtheword-sync, activitydefs-sync, mastery-predicate-sync, blank-engine-weighting-sync — `check:no-emoji` is a **separate** `npm run` script, not part of `build` itself. Playwright at `workers:1` — **101 specs measured this session** (see census; v4's "baseline 60" is stale) — **hard rule, every gate run**: source the env first, `set -a; source .env.local; set +a; npx playwright test --workers=1` — omitting this silently *skips* every `SUPABASE_SERVICE_ROLE_KEY`-gated provisioning spec rather than failing, producing a falsely-reassuring high pass rate (bit `FIX_PARENT_SURFACE_R1`'s first pass directly, see TRAPS) · `idor-proof.mjs` — **29 checks** (measured fresh: `grep -c "check(" scripts/idor-proof.mjs`) — re-run whenever queries touching progress/words/subscriptions change.
- **Design law:** `DESIGN_BRIEF.md` — Candy tokens, Baloo 2 + Quicksand, chunk shadows + press-down, errorless (no red/X), 44px+ targets, NO emoji in shipped UI, placement carve-out §5a. **QA-02 still open**: `LoginScreen.jsx` remains the pre-redesign legacy dark theme — the very first screen every user sees does not use these tokens.

## OPEN ITEMS / NEEDS SAL

### Launch-critical (unchanged since v4 — gate real-money launch)
- **Key rotation (MANDATORY before Stripe live):** exposed Stripe + ElevenLabs + Supabase service-role keys; rotation unconfirmed. Test keys live, so no charge risk today.
- **Counsel email** — drafts ready, send unconfirmed. Longest external clock.
- **Virtual mailbox + business phone** (~$15/mo) → unblocks legal-pages ship + completes counsel package.
- **Gate tokens:** hCaptcha keys → `FLIP: YES`; Google OAuth creds → `GOOGLE: YES`.
- **Spend alerts** (Anthropic/ElevenLabs/Vercel) · **Supabase dashboard** hardening · **CSP** stays Report-Only until a zero-violation walkthrough.
- **Phone/device session** — `DEVICE_TEST_CHECKLIST.md`; iOS Say-It auto-listen still the #1 open product question.
- **Prompt 11 — Stripe-live cutover:** write the moment rotation + counsel are green.

### QA_HARDENING_R1 issues — live status, verified this run (not assumed)
| ID | Sev | Issue | Status |
|---|---|---|---|
| QA-01 | P0 | Home "Let's go!" CTA obscured by fixed BottomNav at 320-375px | **Open** — no report claims a fix |
| QA-02 | P1 | LoginScreen never migrated off the legacy dark theme | **Open** |
| QA-03 | P1 | `ErrorBoundary.jsx` never mounted | **Fixed** — `FIX_NO_BLANK_SCREENS_R1`, item 19 |
| QA-04 | P2 | No catch-all 404 route | **Fixed** — `FIX_NO_BLANK_SCREENS_R1`, item 19 |
| QA-05 | P2 | Galaxy word map unvirtualized (34,152px page at 320w) | **Open** |
| QA-06 | P2 | No UI for banked `streak_freeze_count` | **In progress, not shipped** — built on `feat/quick-wins` (parked mid-Phase-2, unmerged) |
| QA-07 | P3 | Placement vs. Check-In asymmetric network-failure UX | **Open** |
| QA-08 | P3 | `LevelUpCelebration.jsx` is dead code (never rendered) | **Open** |
| QA-09 | P3 | QA audit's own test script isn't idempotent | **Open** (test-script only, not app) |

### Resolved this cycle (kept here briefly for continuity, not re-flagged as open)
- ~~**Migration number collision risk**~~ — **FIXED** by `FIX_MIGRATION_DRIFT_R1` (item 23). `0037`/`0038` are now on `main`, byte-identical to what's applied to production; the `story_fallback` TODO is renumbered to `0039+`; `supabase/migrations/MIGRATIONS.md` states the numbering rule going forward.
- ~~**Parent-surface blindness**~~ — **FIXED** by `FIX_PARENT_SURFACE_R1` + follow-up (item 22). The AI Insight now has visibility into `placement_completed_at` and `stories.read_at`; verified live it no longer claims "hasn't started" when a placement or story-read happened. Note the fix's own scope boundary: word-practice/streak/minutes stat cards are unchanged (correct on their own terms), and `product_events` writes like `scaffold_down` still aren't surfaced anywhere on the Parent Dashboard — see the new item below.

### New this cycle
- **`story_catalog` content contains words outside the 200-word list** (found building `FIX_STORY_FOLLOWUP_R1`'s own test, checked word-by-word against `words`, not assumed). At least "cat" (28-word `vocabulary_used`, 11 of them not literal 200-word entries); the other 19 rows follow the same authored-prose style and are likely similar but were not individually re-checked this cycle. A code-level gate now prevents any *currently-known* violation from reaching a child below the quality floor, but the underlying content still can't be served in that gated path until Sal decides: (a) rewrite the 20 rows to stay strictly in-list (a content edit, pedagogy-adjacent, needs sign-off), or (b) formally accept hand-authored content as exempt from the strict rule (already effectively the decision for the *un-gated* path per `FIX_STORY_FOLLOWUP_R1` — this would just make it official for 100% of catalog-serving, not just the below-floor case). Recommend a full 20-row audit either way.
- **`feat/quick-wins`'s application code is still parked, even though its schema is now on `main`**: item 1 (streak freeze token) has its schema live (`user_streaks.streak_freeze_count`/`freeze_last_granted_at`, both on `main` as of item 23) and functionally shipped to production, but the branch implementing it — `src/lib/streakFreeze.js`, its rule logic, its own tests — *is still not merged to `main`*. Item 2 (sleeping stars) is partial. Items 3–6 (Session Complete framing, "Minutes this week" honesty label, Settings tab-bar clip, password-reset flake) — status not reached/not confirmed by any commit on the branch as of this doc. `FIX_MIGRATION_DRIFT_R1` deliberately did not merge this branch or bring over any of its non-migration code — resuming it still needs its own recon pass first (the branch's own last commit says so). See WORKSTREAM STATE.
- **Migration 0039 (real, future) — `story_fallback` product_events type**: `FIX_STORY_FOLLOWUP_R1`'s `console.warn`-only fallback telemetry (item 21) is a deliberate placeholder for this. Needs: CHECK-constraint migration (now correctly reserved as `0039` per `MIGRATIONS.md`), `api/track.js` allowlist entry if any client-originated write path needs it (current design has this server-adjacent-only, may not need the allowlist at all — re-derive at the time), and per item 16's hard-won lesson, a positive-landing test in the same change, not a follow-up.
- **Catalog coverage audit**: all 20 `story_catalog` rows' actual word-by-word vocabulary against `words` — only "cat" has been individually verified this cycle.
- **`product_events` still has no Parent Dashboard surface at all** (narrower than the old "parent-surface blindness" item, now that the AI Insight specifically is fixed): a parent still can't see "a scaffold-down happened" or any other raw `product_events` activity anywhere in the UI — the AI Insight synthesizes from it into prose, but there's no dedicated events/activity view. Not scoped as its own run yet.
- **Placement polish**: "3-5 minutes" copy vs. measured ~21s-range reality; no progress indicator during the ladder; the two-miss-completes-the-error floor as a still-open pedagogy question for placement specifically (vs. learning activities, where it's settled).
- **Cold-tap `sessionLength` decision** — three named options logged in `ACTIVITY_LOAD_PERF_REPORT.md` (item 20), deliberately untouched pending a perf-vs-pedagogy call from Sal. Separately, the **session-ordering question** (should AI-chosen `sessionLength` exist at all) is explicitly flagged as a pedagogy decision, not a perf one — different owner than the perf options.
- **Free-tier function-word exposure gap** (item 18) — a real, disclosed product gap: free-tier children never see any of the 45 function words regardless of the new placement-floor exemption, since all of them live in units 11-18 and free tier caps at unit 5. Needs a deliberate free-tier-curriculum decision, not a parameter tune.
- **Suite reliability debt**: the flaky/prod-locked-spec pattern is growing as more specs self-provision test accounts and/or override `baseURL` to hit production directly (5 files/14 tests today, see census) — a shared-fixture or serialized-provisioning chore keeps getting deferred across multiple reports' backlogs.

## LAUNCH-CRITICAL SAL-SIDE PATH
Unchanged since v4 (listed here for completeness, not re-derived): counsel email, key rotation (Stripe/ElevenLabs/Supabase service-role, hCaptcha flip, Google OAuth, spend alerts), virtual mailbox + business phone, device test session, Stripe live cutover. See OPEN ITEMS above for the same list with detail.

## TRAPS REGISTRY
Consolidated and deduplicated from every report's own TRAPS section this cycle (`CELEBRATION_FIX_REPORT.md`, `BLANK_ENGINE_REPORT.md`, `ACTIVITY_LOAD_PERF_REPORT.md`, `NO_BLANK_SCREENS_REPORT.md`, `PEDAGOGY_CALIBRATION_REPORT.md`, `PLACEMENT_CHECKIN_REPORT.md`, `STORY_QUALITY_REPORT.md`, `STORY_FOLLOWUP_REPORT.md`, `QA_HARDENING_REPORT.md`) plus v4's own "HARD-WON SESSION RULES." Grouped by theme, not chronological.

**Environment / worktrees**
- `.env.local` is gitignored and does not carry over into a fresh `git worktree add` — copy it in manually before `npm run build`/`preview`/any Supabase-touching script, every time.
- `git worktree` is the fast, non-destructive way to check "does this failure pre-exist on `main`" (or on a fresh checkout at all) without disturbing your current branch's state — used repeatedly this cycle to separate real regressions from pre-existing flakiness.
- `git checkout <ref> -- .` (working-tree-only, no branch switch) is a lighter-weight variant of the same idea inside one worktree — remember to `git checkout HEAD -- .` immediately after, and `git status --short` before/after both times.
- Never edit source files while a background full-suite Playwright run against the same working tree is still executing — even across a `git commit`. The dev server serves live file contents from disk, not the git-committed snapshot.
- **The primary worktree (`~/magic-words`) can sit on a stale feature branch for days**, and `main` itself may already be checked out in a *different* `.claude/worktrees/<name>` directory (recurring this cycle — `git checkout main` fails with "already checked out" from the primary worktree; work around by branching directly off the ref, `git checkout -b <new-branch> main`, without ever occupying the `main` slot, then merge from inside the worktree that already has `main` checked out). Combined, this means **any "does file X exist" check must first confirm which branch is checked out** — a file committed and merged to `main` hours ago can correctly, non-buggily appear "missing" from `~/magic-words` if that worktree happens to be sitting on an older, unrelated branch at the moment of the check. This exact pattern produced a full false-alarm "lost artifact" report this cycle (`DOCS_MASTER_V5_R2`) — the file was never lost, `feat/quick-wins` (parked since before the file existed) was just the checked-out branch when someone ran `ls docs/`.
- `git stash push -u` puts untracked files in a **separate** parent commit (`stash@{N}^3`), not the stash's main tree — `git checkout stash@{N} -- <path>` alone will not restore an untracked file; target `stash@{N}^3` explicitly.
- Running the full Playwright suite twice back-to-back in one session can produce two *different* small failure sets with zero overlap (99/101 then 98/101, this cycle) rather than the same failure twice — plausibly AI-endpoint rate-limit pressure (`session-generator` 10/min, `story-engine` 4/day, shared per identity) rather than flakiness alone. Budget for one full run when possible; don't chase a third run assuming it'll go green.

**Supabase / product_events / migrations**
- Adding a new client-facing event name to `api/track.js`'s allowlist is NOT sufficient by itself — the `product_events_event_type_check` CHECK constraint (a migration) must land in the *same* change, or inserts silently fail (fire-and-forget, `console.error` only, HTTP 200 either way). Bit `scaffold_down` once already; the lesson is now explicitly load-bearing for every future event type.
- Two client-originated event types in the same PR/migration need BOTH the CHECK constraint AND the `api/track.js` allowlist entry in one commit; a server-only event type (like `checkin_started`) needs only the CHECK constraint.
- `child_profiles`'s table-level `REVOKE UPDATE` (migration 0033) only blocks `authenticated`/`anon` roles, never `service_role` — a real, useful affordance for seeding otherwise-client-unwritable test-fixture columns directly via the admin client.
- Supabase selects cap at 1000 rows — page with `.range()` or truncate silently (bites the most active kids first).
- A migration file's number is only unique per-branch until merge — check unmerged branches (not just `main`) before numbering a new one; this cycle found a live collision (see OPEN ITEMS).

**Testing / Playwright**
- `SUPABASE_SERVICE_ROLE_KEY` must be exported in-shell — `npx playwright test` without it silently *skips* every account-provisioning spec (`test.skip(!SERVICE_KEY,...)`) rather than failing, producing a falsely-reassuring high pass rate. Compare pass/fail counts across runs only when this was set identically both times.
- A production-baseURL-locked spec (`test.use({baseURL: "https://200magicwordsapp.com"})`, or `DEPLOY_BASE_URL` defaulting there) can never exercise an unmerged branch's code — check for this override before blaming your diff for a spec failure.
- `waitFor({state:'hidden'})` resolves immediately for a never-attached element, not just an invisible one — always wait for a positive rendered signal instead of a negative one.
- `fullPage: true` Playwright screenshots smear `position:fixed` elements across stitched scroll frames — never trust an apparent fixed-nav overlap from a full-page screenshot alone; re-capture `fullPage:false` at the real scroll position before logging it as a bug.
- Fixed `waitForTimeout` delays are a known source of flaky screenshots/assertions — prefer a real element-visibility/enabled-state wait every time, even when it's more code.
- WordArt/tile accessible names combine icon + visible text (e.g. "cat cat") — exact-match role queries fail; use an anchored or unanchored regex instead.
- Ambiguous button text (e.g. two elements both literally named "Sign in") throws a Playwright strict-mode violation — disambiguate via a more specific selector (`button[type="submit"]`) rather than relying on name alone.
- `admin.generateLink` produces implicit-flow links that PKCE rejects — use `verifyOtp({token_hash})` instead. Admin-created fixtures still need `parental_consent` metadata or they hang on the COPPA interstitial.
- A Playwright hard test-timeout aborts without unwinding `try/finally` — cleanup can be skipped specifically in a timeout-abort case; check for orphaned accounts after any timeout failure.
- A shell command with backticks inside a single-quoted `git commit -m "..."` string will command-substitute — avoid raw backticks in commit messages passed via `-m`.

**App-specific gotchas**
- Modules importing `supabaseClient.js` can't load under Playwright's/Node's plain module loader — extract pure logic to zero-import files in `src/lib/` (the established pattern: `masteryCalibration.js`, `starKeeper.js`, `checkinEligibility.js`, `streakFreeze.js`).
- `import.meta.env` inlines at dev-server start — env-gated UI needs its own Vite instance to test a different flag state.
- The Grown-Ups hold-gate needs real Playwright `page.mouse.down()`/`up()` around a wait — its rAF timer doesn't advance under synthetic `dispatchEvent`.
- Vite dev-mode's native-ESM live-binding throws hard on a missing named export (even if unused); Rollup's production build silently tolerates the same gap. A stale dead import can crash the *entire* dev-mode React tree (every route) while `npm run build` and every production walk stay green — invisible until someone runs the dev server. Cost one workstream over an hour of misdiagnosis (initially blamed on memory pressure) before console-capture revealed the real cause.
- A pool/candidate array's element ORDER matters when later truncated with `.slice(0, N)` — appending a small reserved sample to the end of an unbounded-source array risks silent starvation of that sample; verify it survives the cap with a live call, don't assume.
- The local client-fallback session plan and the real server-side `api/session-generator.js` select candidate words differently (sort order, mastered-word inclusion) — a fixture built against one won't reproduce the same shape against the other.
- Rate limits are shared per authenticated user across every mode of a multi-mode endpoint (`session-generator`'s 10/min covers plain/`reviewOnly`/`placementMode`/`checkinMode`/`historyMode` alike) — a verification script spending one identity's budget on negative-path checks can starve a positive multi-step flow on the same token.
- `.select()` queries in test/audit scripts should verify against the LIVE table state at cleanup/verification time, not trust an earlier report's snapshot — an unexplained account cluster is a real signal worth re-checking, not noise to ignore.
- Never trust a "hand-authored"/"curated" label as an automatic guarantee of "vocabulary-safe" or otherwise-compliant — verify content word-by-word against the actual rule, the same rigor applied to generated content.
- A pool of size 1 (or an empty mastered-word list) is not an edge case for this app — it is the default state of every brand-new account under the mastery-gate floor. Any future feature that personalizes from "mastered words" must design for that as the *common* first case.

## WORKSTREAM STATE
- **Merged to `main` since v4** (chronological): `fix/celebration-timing` (`7e355dc`) → `feat/pedagogy-calibration` (`633d68e`) → `feat/placement-checkin` (`b865400`) → `feat/blank-engine` (`04e374b`) → `fix/no-blank-screens` (`4b2dfd0`) → `perf/activity-load` (`93bf99c`) → `fix/story-quality` (`9115eb0`) → `fix/story-followup` (`eac7a0b`) → `docs/master-v5` (`714b3b3`, R1's own merge) → `fix/parent-surface` (`489d959`) → `fix/parent-surface-followup` (`97a16f4`) → `fix/migration-drift` (`29ecc2b`, then a closing docs commit `117327b`).
- **Artifact-only, never intended to merge**: `qa/e2e-audit` (QA_HARDENING_R1's report + screenshots live only here, per its own read-only/no-fix mandate).
- **Parked, unmerged, live-schema implications**: `feat/quick-wins` (Package E) — still 3 commits ahead of `main`, last one explicitly `WIP: Package E phase 2 parked`, unchanged since v5's prior revision. Item 1 (streak freeze)'s **migration files are now reconciled onto `main`** (`FIX_MIGRATION_DRIFT_R1`, item 23 — the collision/drift risk this doc previously flagged is resolved), but the branch's own application code implementing that schema (`src/lib/streakFreeze.js` and friends) is still not merged; item 2 (sleeping stars) partial; items 3-6 status not reached per the branch's own commit history. **Resuming this branch still needs a recon pass first** (the branch's own last commit says so, and `FIX_MIGRATION_DRIFT_R1` deliberately did not touch or merge any of its non-migration code) — don't assume its 3-commit state is current without re-checking `user_streaks.streak_freeze_count`/`freeze_last_granted_at` against `main`.
- **`docs/master-v5-r2`** (this run) — docs-only refresh of this file plus its own report, `docs/DOCS_MASTER_V5_R2_REPORT.md`.
- **Other branches present in the repo, not part of this cycle's work, not audited here**: `qa/pedagogy-preview-walk`, `chore/legal-pages` (holds the rendered-but-unshipped legal pages per item 10). Recommend a separate pass to confirm which, if any, are safe to delete — not done as part of this docs-only run (out of scope; this doc only recommends, never deletes).

## BACKLOG (post-launch order: B → C → E → D → later — B and C are now DONE, not backlog)
- ~~**B — Pedagogy calibration**~~ — **DONE**, see item 16.
- ~~**C — Placement report + Star Check-In**~~ — **DONE**, see item 17.
- **E — Quick wins** — **IN PROGRESS, PARKED**, see WORKSTREAM STATE. Migration files reconciled to `main` (item 23); application code remaining: unit-relative Session Complete framing, sleeping stars (partial), "Minutes this week" honest label, Grown-Ups tab-bar clip at 375px, password-reset `verifyOtp` flake diagnose-then-fix.
- **D — Admin panel v1** — separate private Vercel project, read-only first. Not started.
- **Later:** Sparks spend path, Session Complete redesign (A2), WordArt Batch 3, referral/gift/digest/certificates, Educator Loop, PWA/offline, accessibility, staging Supabase, delete `/app-legacy`, `story_time` chrome migration, test-suite provisioning-contention chore (growing, see OPEN ITEMS' suite-reliability-debt note), QA-01/02/05/06/07/08/09 fixes (see the QA status table above), catalog-vocabulary audit/decision, migration 0039 `story_fallback` event type, `product_events` Parent Dashboard surface (new this cycle, see OPEN ITEMS).

## COMPLETION ESTIMATE
Child Loop ~90% (story-quality/followup + blank-engine's sixth skill closed real gaps) · Parent Loop ~87% (up from ~85% — `FIX_PARENT_SURFACE_R1` closed the AI Insight's truthfulness gap; still open: no dedicated `product_events` activity view) · Educator ~10% · Institution 0%. **Launchable paid product: engineering ~95% — remaining gap is Sal-gated (rotation, counsel, mailbox, tokens) + Prompt 11**, unchanged from v4's own estimate; this cycle's work (parent-surface truthfulness fix, migration-drift reconciliation) was hardening/correctness on the already-built product, not new launch-blocking surface area.

## SOURCES
Every major section above traces to one of these. Report path is relative to `docs/` unless noted as branch-only.

| Section(s) | Source | Location |
|---|---|---|
| Items 1–13 (carried forward), Key Reference base facts | `200MW_Master_Project_Doc_v4.md` | Committed this run (previously only in a local Downloads staging area, never in the repo) |
| Item 14, QA status table | `QA_HARDENING_REPORT.md` | **`qa/e2e-audit` branch only, not on `main`** — `git show qa/e2e-audit:docs/QA_HARDENING_REPORT.md` |
| Item 15 | `CELEBRATION_FIX_REPORT.md` | `main`, merge commit `7e355dc` |
| Item 16 | `PEDAGOGY_CALIBRATION_REPORT.md` | `main`, merge commit `633d68e` |
| Item 17 | `PLACEMENT_CHECKIN_REPORT.md` | `main`, merge commit `b865400` |
| Item 18 | `BLANK_ENGINE_REPORT.md` | `main`, merge commit `04e374b` |
| Item 19 | `NO_BLANK_SCREENS_REPORT.md` | `main`, merge commit `4b2dfd0` |
| Item 20 | `ACTIVITY_LOAD_PERF_REPORT.md` | `main`, merge commit `93bf99c` |
| Item 21 | `STORY_QUALITY_REPORT.md`, `STORY_FOLLOWUP_REPORT.md` | `main`, merge commits `9115eb0`, `eac7a0b` |
| Item 22 | `PARENT_SURFACE_REPORT.md` (includes its own CORRECTION + FOLLOW-UP section) | `main`, merge commits `489d959`, `97a16f4` |
| Item 23 | `MIGRATION_DRIFT_REPORT.md` | `main`, merge commits `29ecc2b`, `117327b` |
| Fresh census table | Direct measurement this session (`DOCS_MASTER_V5_R2`, 2026-07-13) | Commands cited inline in the table; full detail in `DOCS_MASTER_V5_R2_REPORT.md` PHASE 2 |
| Forensics on this doc's own "artifact loss" report | Direct measurement this session | `DOCS_MASTER_V5_R2_REPORT.md` PHASE 1 — full evidence chain (`git log --all`, `git merge-base --is-ancestor`, reflog, R1's own "no separate report file" design note) |
| Workstream state | `git log --oneline --merges 2afd849..HEAD` (v4-era anchor commit → `HEAD`), plus `feat/quick-wins`'s own `FEAT_QUICK_WINS_R1.md`/`QUICK_WINS_REPORT.md` (branch-only, not on `main`) | This session + R1's session |
| Traps registry | All reports listed above, deduplicated by theme, plus this session's own forensics findings | This session + R1's session |

**Note on report-location discrepancies**: two reports referenced by this doc's own prompt (`docs/DOCS_MASTER_V5_R1.md`) live only on branches, not on `main` — `QA_HARDENING_REPORT.md` (`qa/e2e-audit`) and the two Package-E docs (`feat/quick-wins`). This is disclosed, not silently normalized: `main`'s `docs/` folder does not contain these files, and a future reader who only checks out `main` will not find them without checking those branches explicitly.
